import { assert } from 'chai';

describe('POST brand', () => {
    describe('valid', () => {
        it('xxx', (done) => {
            done();
        });
    });

    describe('error', () => {
        it('xxx', (done) => {
            done();
        });
    });
});